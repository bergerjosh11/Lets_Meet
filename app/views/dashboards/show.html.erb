<h1> Hey Friends, Let's Meet </h1>
<body>
<ul> Here we will find and plan events for you and your buddies to do around town</ul>
  </ul>
</body>
<div>
<%= button_to "Add Event", new_event_path, method: :get %>
</div>
<%@events.each do |event|%>
  <ul>
    <div>
      <li><h4><%= event.name %></h4></li>
      <div>
        <%= event.location %>
      </div>

      <div>
        <%= event.date %>
      </div>

      <div>
        <%= event.time %>
      </div>

      <div>
        <%= link_to "Edit Event", edit_event_path(event), method: :get %>
      </div>
  </ul>
<% end %>
